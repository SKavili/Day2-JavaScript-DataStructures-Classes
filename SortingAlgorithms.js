let numbers = [3, 1, 4, 1, 5, 9, 2, 6];
numbers.sort((a, b) => a - b); // Numerical ascending order
console.log(numbers);


let letters = ['a', 'b', 'c', 'd'];
letters.reverse(); // Reverses the order: ['d', 'c', 'b', 'a']

let words = ['apple', 'banana', 'cherry', 'date'];
words.sort((a, b) => a.length - b.length); // Sort by string length
